# API CONTRACTS

## GET /api/telemetry

Returns all telemetry signals

## POST /api/telemetry

Adds telemetry signal

## GET /api/runtime

Returns runtime logs

## POST /api/runtime

Adds runtime log
