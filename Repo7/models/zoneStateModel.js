// Reserved for MongoDB / Mongoose upgrade later

/*
const mongoose = require("mongoose");

const ZoneStateSchema = new mongoose.Schema({
  zone_id: Number,
  risk_score: Number,
  level: String,
  trend: String,
  behavior: String,
  timestamp: Number
});

module.exports = mongoose.model("ZoneState", ZoneStateSchema);
*/